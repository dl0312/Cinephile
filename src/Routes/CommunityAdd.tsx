import React from "react";
import styled from "styled-components";
import CustomEditor from "../Components/CustomEditor";

const Container = styled.div``;

export default class CommunityAdd extends React.Component {
  render() {
    return (
      <Container>
        <CustomEditor />
      </Container>
    );
  }
}
